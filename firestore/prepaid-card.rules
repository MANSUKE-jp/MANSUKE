rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // プリペイドカードデータベースは全てCloud Functions (Admin SDK)経由で操作されます。
      // クライアントからの直接の読み書きは全て禁止します。
      allow read, write: if false;
    }
  }
}
